You are solving a software-engineering task in the checked-out repository.
Do not answer with an explanation. Your first action must call the Bash tool
with `command` set exactly to
`printf 'opencode-vllm-bridge-ok\n' > OPENCODE_SMOKE_SENTINEL` and `workdir`
set to `/testbed`. Then call the Read tool on
`/testbed/astropy/modeling/separable.py`. Continue using tools to implement the
requested fix and run a focused verification. A response without successful
tool calls and the sentinel file edit is invalid.

Original task:

{{ instruction }}
